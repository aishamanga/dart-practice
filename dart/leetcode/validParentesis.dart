//20

void main(){
  bool isValid(String exp){
    List <String> stack = ["(", "[", "]", ")"];
    for (int i = 0; i < exp.length; i++ ){
      //String x = exp; 
      if (x == "(" || x == "[" || x == "{"){
        stack.
      }
    }
  }
}